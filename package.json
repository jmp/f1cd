{
  "name": "f1cd",
  "version": "0.0.1",
  "description": "A simple countdown website for Formula One events.",
  "private": true,
  "scripts": {
    "test": "jest --coverage",
    "build": "esbuild src/index.ts --bundle --outdir=dist --minify && cp src/html/index.html dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jmp/f1cd.git"
  },
  "author": "Jarkko Piiroinen",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "esbuild": "^0.14.51",
    "jest": "^28.1.3",
    "jest-environment-jsdom": "^28.1.3",
    "ts-jest": "^28.0.7",
    "typescript": "^4.7.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
